# Staging environment configuration.
# BASE_URL is the ORIGIN only (scheme + host, no path, no trailing slash) —
# Page Objects append their own routes.
BASE_URL=https://uat.osool.cloud
BASE_API=
API_KEY=
LANGUAGE=en
SUPERADMIN_EMAIL=superadmin@osool.sa
SUPERADMIN_PASSWORD=Tarqeem21
